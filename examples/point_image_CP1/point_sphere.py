N = 24     # Create N*N images
amp = 1
sd = .5

# Do some kind of loop over the N*N grid
# Place a Gaußian at the point selected

# This example is actually tricky because we will need to apply EM
# coordinates to make this into a proper sphere, and not just something
# with spherical homology.
